# Path to IFCB data directory on host
COMPOSE_IFCB_DATA_DIR=/path/to/ifcb/data

# Path to directory containing classifier.pkl on host
COMPOSE_MODEL_DIR=/path/to/models

# Port for the service (default: 8001)
COMPOSE_PORT=8001
